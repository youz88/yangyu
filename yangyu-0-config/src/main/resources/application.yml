server:
  port: 8008
spring:
  profiles:
      active: native
  cloud:
      config:
        server:
          native:
            search-locations: classpath:/config #配置文件根目录
  security:
    user:
      name: admin
      password: admin